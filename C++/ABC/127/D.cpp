#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    vector<int> v;
    for (int i = 0; i < n; ++i) {
        int a;
        cin >> a;
        v.push_back(a);
    }

    for (int i = 0; i < m; ++i) {
        int b, c;
        cin >> b >> c;
        
    }
}