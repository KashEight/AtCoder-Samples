#include <bits/stdc++.h>

using namespace std;

int main() {
    int N, K;
    vector<int> v(N);
    vector<int> own_j;
    for (int i = 0; i < N; ++i) cin >> v[i];

    for (int i = 0; i < K; ++i) {
        for (int j = 0; j < 100 - i; ++j) {
            
        }
    }
}